export interface libros {
    id?: string;
    titulo: string;
    fecha: string;
    genero: string;
    descripcion: string;
}

export interface recomendados {
    id?: string;
    titulo: string;
    fecha: string;
    autor: string;
}